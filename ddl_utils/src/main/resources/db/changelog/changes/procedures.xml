<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!-- One changeset per stored procedure, mirroring functions.xml. Each
         procedure lives in its own file under procedures/<schema>/ and is added
         with createProcedure. Intentionally empty for now: add a changeset with
         the createProcedure and its rollback here once the first procedure
         exists. -->
    <changeSet id="020-create-procedures" author="ddl_utils" dbms="postgresql" runOnChange="true">
    </changeSet>
</databaseChangeLog>
