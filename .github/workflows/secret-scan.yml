name: Secret Scan

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  secret-scan:
    # Pinned to the workflow_library commit; bump the SHA deliberately when
    # upgrading the library.
    uses: eyupmiduck/workflow_library/.github/workflows/secret-scan.yml@7fa94c847818d1d45df7132d6cc3857bf6162f61
